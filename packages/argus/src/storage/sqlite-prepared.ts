/**
 * SQLite + FTS5 — persistência do índice estrutural (S06, PRD D1).
 * Dependência opcional: better-sqlite3 (native addon).
 */
export const SQLITE_PREPARED = true;
export const SQLITE_SCHEMA_VERSION = "1.0.0";
