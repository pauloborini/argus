/**
 * SQLite preparado para S06+ — sem persistência funcional nesta sprint (PRD D1).
 * Dependência opcional: better-sqlite3 (native addon, schema em sprint futura).
 */
export const SQLITE_PREPARED = true;
export const SQLITE_SCHEMA_VERSION = null as string | null;
