{
  "name": "argus",
  "version": "2.3.1",
  "private": true,
  "description": "Argus — motor local de code retrieval e context packing para agentes",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@owerride/argus",
    "benchmark:mvp": "npm run benchmark:mvp --workspace=@owerride/argus",
    "test": "npm run test --workspace=@owerride/argus",
    "lint": "npm run lint --workspace=@owerride/argus",
    "typecheck": "npm run typecheck --workspace=@owerride/argus",
    "docs:check": "node scripts/check-public-docs.mjs",
    "validate": "npm run docs:check && npm run typecheck && npm test && npm run lint && npm run build",
    "smoke:package": "node scripts/smoke-package.mjs",
    "homologate": "npm run build && node scripts/homologate.mjs",
    "release:check": "node scripts/check-release.mjs",
    "release:manual": "node scripts/manual-release.mjs",
    "release:eval": "npm run build && node scripts/release-evaluation.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
