.env
.env.*

# Node / TypeScript
node_modules/
dist/
dist-release/
.argus/
*.tsbuildinfo
*.tgz

# Atlas Ecosystem / local tooling
.talos
.athena
.claude

# IDEs / ferramentas locais (não versionar configs pessoais)
.cursor/
.codex/
.agents/

# Processo local: guides, brainstorming, prd, references, private, archive
# (só o .gitignore do .app-work é versionado — schema AppVault)
.app-work/*
!.app-work/.gitignore

# OS
.DS_Store
