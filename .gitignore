# GitHub Actions nao roda neste repo: gates rodam localmente.
.env
.env.*

# Node / TypeScript
node_modules/
dist/
dist-release/
.argus/
*.tsbuildinfo
*.tgz

# Atlas Ecosystem / local tooling
.talos
.athena
.claude

# IDEs / ferramentas locais (não versionar configs pessoais)
.cursor/
.codex/
.agents/

# Processo local: guides, brainstorming, prd, references, private, archive, issues
# (só o .gitignore, o mapa, o roadmap e o state do kit são versionados — schema AppVault)
.app-work/*
!.app-work/.gitignore
!.app-work/INDEX.md
!.app-work/roadmap/
!.app-work/hephaestus-state.json

# Checkpoint efêmero do kit (hephaestus)
.hephaestus/

# OS
.DS_Store
